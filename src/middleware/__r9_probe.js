// SANDBOX ARTIFACT — safe to delete. Phase 20.b R9-ratchet negative test.
// The FUSE mount blocks unlink, so this file is neutralized (no SQL, no HMAC) instead.
// Remove host-side: `git clean -f src/middleware/__r9_probe.js` or delete it.
module.exports = undefined;
